#!/bin/sh
# Fast optional pre-commit hook. CI remains the authoritative full gate.
# Enable once with: git config core.hooksPath .githooks .
# Bypass in an emergency with --no-verify.
exec make fmt-check
