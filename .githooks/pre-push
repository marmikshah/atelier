#!/bin/sh
# Canonical pre-push hook — mirrors CI: checks + tests.
# Enable once with: git config core.hooksPath .githooks  (or `make hooks`).
# Bypass in an emergency with --no-verify.
set -e
make pre-commit-checks
make test
