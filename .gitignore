/target/
.atelier/
.DS_Store
*.swp
.claude/
.venv/
resume.sh
docs/ART-QUALITY-REVIEW.md
.env

# recipe-output art lands at repo root on replay; never commit it (docs/ art is tracked)
/*.gif
/*.png

# Libraries don't commit lockfiles — the consumer resolves.
Cargo.lock

# pixel-sheet export sidecars (page reads only runs.json)

# benchmark run artifacts (transcripts, isolated homes, exported art)
benchmarks/runs/
research/
__pycache__/
