/target/
.atelier/
.DS_Store
*.swp
.claude/
.venv/
resume.sh
docs/ART-QUALITY-REVIEW.md
.env

# recipe-output art lands at repo root on replay; never commit it (docs/ art is tracked)
/*.gif
/*.png

# This workspace ships an executable, so Cargo.lock is committed. Release,
# Docker, CI, and source-install builds use --locked.

# Legacy local Pages outputs from pre-extraction checkouts. The canonical site
# and benchmark corpus now live in marmikshah/atelier-site.
/site/tools.html
/site/showcase/runs.json

# Legacy benchmark run artifacts (transcripts, isolated homes, exported art).
benchmarks/runs/
__pycache__/
